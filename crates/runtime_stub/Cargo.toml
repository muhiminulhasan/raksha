[package]
name = "runtime_stub"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
raksha-core = { path = "../raksha-core" }
